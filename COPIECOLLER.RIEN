<?php
$req = $connexion->prepare('SELECT title, content FROM articles  WHERE user_id = :user_id AND is_DELETED =0  ORDER BY id DESC');
$req->execute(array(
    'user_id' => $user_id
));

$articles = $req->fetchAll();

foreach ($articles as $article) {
?>
    <div class="">
        <h2><?php echo $article["title"]; ?></h2>
        <p><?php echo substr($article["content"], 0, 400); ?>... </p>
        <a class="Ahref" href="">Lire la suite</a>
    </div>
<?php
}
?>